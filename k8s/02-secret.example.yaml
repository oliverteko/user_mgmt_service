# Template only - DO NOT put real secret values in a committed file.
#
# Create the real secret imperatively instead:
#
#   kubectl create secret generic app-secret -n user-mgmt \
#     --from-literal=DB_USERNAME=postgres \
#     --from-literal=DB_PASSWORD='<real-password>' \
#     --from-literal=JWT_SECRET='<real-jwt-secret>'
#
# See k8s/README.md for details and alternatives.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: user-mgmt
type: Opaque
stringData:
  DB_USERNAME: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
