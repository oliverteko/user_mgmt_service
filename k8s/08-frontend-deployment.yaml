apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: user-mgmt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/oliverteko/user-mgmt-service-frontend:latest
          ports:
            - containerPort: 3000
          env:
            # NEXT_PUBLIC_API_URL is intentionally NOT set here: it is baked into
            # the client JS bundle at image build time (see frontend/Dockerfile
            # and .github/workflows/build-and-push.yml) and has no effect as a
            # runtime env var.
            - name: INTERNAL_API_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: INTERNAL_API_URL
          # "/" is not gated by middleware.ts and doesn't depend on the backend,
          # so it's a safe, unauthenticated probe target
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
